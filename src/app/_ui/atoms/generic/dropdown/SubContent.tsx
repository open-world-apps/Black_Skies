'use client';

import styled from 'styled-components';
import { SubContent as SC } from '@radix-ui/react-dropdown-menu';
import { content } from './sharedStyles';

const SubContent = styled(SC)`
  ${content}
`;

export default SubContent;
