'use client';

import { styled } from '@/lib/configs/stitches.config';

const RightSlot = styled('div', {
  marginLeft: 'auto',
  paddingLeft: '20px',
  color: 'Violet',
});

export default RightSlot;
