'use client';

import styled from 'styled-components';
import { Content as C } from '@radix-ui/react-dropdown-menu';
import { content } from './sharedStyles';

const Content = styled(C)`
  ${content}
`;

export default Content;
