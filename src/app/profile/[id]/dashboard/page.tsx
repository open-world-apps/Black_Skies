import React, { FC, ReactElement } from 'react';

// Dashboard UI pending redesign — old organism removed in the intake/landing rebuild.
const Page: FC = (): ReactElement => (
  <main>
    <div
      style={{
        minHeight: '100vh',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        fontFamily: 'var(--bs-mono)',
        letterSpacing: '0.18em',
        color: 'var(--bs-ink-dim)',
      }}
    >
      // DASHBOARD — REDESIGN PENDING
    </div>
  </main>
);

export default Page;
